services:
  web:
    image: nginx:latest
    ports:
      - '9090:80'
    volumes:
      - ./:/var/www
      - ./default.conf:/etc/nginx/conf.d/default.conf
  php-fpm:
    image: php:8-fpm
    volumes:
      - ./:/var/www